import { FunctionComponent } from 'react';
import Template from '@components/common/Template';

const Posts: FunctionComponent = () => {
	return <Template> This is Posts Page</Template>;
};

export default Posts;
