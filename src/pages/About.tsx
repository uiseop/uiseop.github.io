import { FunctionComponent } from 'react';
import Template from '@components/common/Template';

const About: FunctionComponent = () => {
	return <Template>This is About 섭이</Template>;
};

export default About;
